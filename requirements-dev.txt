# Dev-only extras for lab property tests. Not a runtime Fog dependency.
# pip install --user -r requirements-dev.txt
# Pinned to the version protocol-invariants CI installed (2026-08-29).
hypothesis==6.165.10
