Daily academy exam artifacts (SCA/ACB).

See `docs/ACADEMY-DAILY-EXAMS.md` and `schema.json`.

Layout: `YYYY-MM-DD/{roster copy via ../roster.json, exam, scores, teachers}.json` + `latest.json`.

Primary writer: Mac Fog `ops/desk-collegium/academy_exams.py` via desk cycle / LaunchAgent — **Bot not required**.
