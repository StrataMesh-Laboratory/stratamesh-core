# Pages fail-open (1027). Never SPA-rewrite hop-owned paths to index HTML.
# Live 2026-09-02 PT: /atelier and /api/health were 200 same HTML as /;
# Do NOT add /* /index.html 200. Do not SPA-rewrite /chat.
# /sandbox is the sandbox document (sandbox.html: chrome + #atelierFrame → same-origin /gnu-atelier.html).
# /dashboard is the workbench (dashboard.html). They are not the same HTML.
# /atelier remains 404 on apex Pages (renderer lives on same-origin /gnu-atelier.html).
# Functions under functions/api remain in-tree; _routes.json excludes /api/*
# so Pages does not serve landing HTML (or fake JSON) on /api/health.
# /chat is chat.html via Pages clean URL (no /chat → /chat.html 200 rewrite — that 308-loops).
/manutencao /maintenance-1xxx.html 200
/atelier /gnu-atelier.html 200
/atelier/ /gnu-atelier.html 200
/api/* /404.html 404
# Pages auto-serves foo.html at /foo (clean URL) and 308s /foo.html → /foo.
# NEVER add /foo /foo.html 200 — that fights the 308 and loops (308 Location: /foo).
# Keep only true aliases / intentional 404s below.
/oss-pass-atelier-mud-oz /oss-pass.html 200

# Olissippo lore stage (Phase 6) — served as frontend/olissippo.html via Pages clean URL.
# No /olissippo rewrite. Trailing slash: let Pages 308 /olissippo/ → /olissippo.

# Lusitania player briefing — frontend/lusitania.html via Pages clean URL.
# No /lusitania rewrite (same lesson as /olissippo: avoid fighting Pages 308).
