# Pages fail-open. Never SPA-rewrite hop-owned paths to index HTML.
# Do NOT add /* /index.html 200.
# Do NOT 200-rewrite /path → /path.html — fights CF Pages pretty-URL (308 loops).
# /atelier remains 404 on apex (renderer is /gnu-atelier.html).
# Functions: _routes.json include only /api/*.
/manutencao /maintenance-1xxx.html 200
/atelier /404.html 404
/atelier/* /404.html 404

# Lang homes
/pt /pt/ 302
/pt/index /pt/ 302

# EN aliases → PT fog docs (same HTML)
/en/fog-appliance /fog-appliance 302
/en/fog-infrastructure /fog-infrastructure 302
/en/fog-stack /fog-stack 302
/mapa /roadmap 302
