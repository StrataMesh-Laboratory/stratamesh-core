# Copy to /etc/stratamesh/publish.env on the Oracle VM (mode 0600).
# Never commit a real STATUS_TOKEN. The systemd unit loads this file optionally:
#   EnvironmentFile=-/etc/stratamesh/publish.env
STATUS_TOKEN=
STATUS_INGEST_URL=https://stratamesh-status.stratamesh.workers.dev/ingest
