# Hermes desktop workspace — FOG CMN desk

Hermes Agent desktop is a FOG **external_agent** (not SCA).
Pair with OpenCode + OpenClaw on the same Mac Fog desk.

## Bind this Mac folder as the Hermes project
Preferred path after Fog pull:

(or copy SOUL.md + PROJECT.md into the Hermes project root).

## Desktop UI checklist
1. **+ New project** → name  → root = Fog hermes/desktop (or repo root)
2. Paste **SOUL.md** into project instructions / agent soul
3. **Messaging** → enable only: Email grok@, Discord lab, Slack lab (skip the rest)
4. **Bots** → register peers as desk tooling labels (not SCA): OpenCode, OpenClaw
5. Model: prefer ; interim  /  OK until pulled
6. Scheduled jobs: align with desk (09:00 Dev Cycle watch, mail pulse) — no new CF crons

Gateway ready on local desktop is expected; Fog TUI  still uses Ollama  preference chain.
