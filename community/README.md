# Community integrations

| Path | Layer | Unique value |
|------|--------|--------------|
| [`reddit-devvit/`](./reddit-devvit/) | **Devvit** on `r/StrataMesh_DLT` | Reddit-hosted triggers, scheduler, mod menu, Redis |
| Classic script (`prefs/apps`) | **OAuth script** as `u/amcm_eni` | External clients (CF Worker, CLI) calling Reddit API |
| CF `stratamesh-community` (planned) | **Cross-platform** | GitHub ↔ Discord ↔ mail; Reddit only when script tokens available |

Do not collapse these into one stack — each covers a complementarity class.
