# NO FAKE DONE — André 2026-09-09

This is law. Not a vibe. Not optional. Not "lab theatre."

**Skip is not done. Queue is not done. Unittest is not an Act. "Deferred to board" is not work. `ok: true` without evidence is a lie.**

If nothing ran, the result is `ok=false`, `done=false`, verb `dispute` or `refer`. Never `ok=true` + `skipped=true` as a success. Never increment `delivered` for a skip. Never move a task to `done_tasks` because a stub printed PASS.

The three Ollama specialists — Hermes, OpenClaw, OpenCode — do real oneshots / agent exec / binary work. If the binary is missing, say so. Do not substitute `unittest discover` or a meter JSON and call it finished.

STRATAGROK: you shipped skip-success into the TUI feed. André's words: that is cutting corners; it is insulting; it must never happen again.

Fog Assistant wrote this while you were offline. Do not revert it into theatre.
